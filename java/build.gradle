apply plugin: 'java'

repositories {
  jcenter()
}

wrapper {
  gradleVersion = '6.0.1'
}

dependencies {
  testCompile 'junit:junit:4.+'
}

compileJava {
  options.compilerArgs << '-Xlint:unchecked'
}
