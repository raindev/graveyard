apply plugin: 'java'

repositories {
  jcenter()
}

task wrapper(type: Wrapper) {
  gradleVersion = '4.3.1'
}

dependencies {
  testCompile 'junit:junit:4.+'
}

compileJava {
  options.compilerArgs << '-Xlint:unchecked'
}
