apply plugin: 'java'

repositories {
  jcenter()
}

wrapper {
  gradleVersion = '4.5'
}

dependencies {
  testCompile 'junit:junit:4.+'
}

compileJava {
  options.compilerArgs << '-Xlint:unchecked'
}
